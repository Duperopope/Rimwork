CHANGE: Add a method to check if a zone is functional.
FILE: src/RimWorldLab.Core/GameWorld.cs
<<<<<<< SEARCH
public bool IsTilePartOfRoom(int x, int y)
{
    // Check if the tile is part of any room
    foreach (var zone in Zones)
    {
        if (zone.Contains(x, y))
        {
            return true;
        }
    }
    return false;
}
=======
public bool IsTilePartOfRoom(int x, int y)
{
    // Check if the tile is part of any room
    foreach (var zone in Zones)
    {
        if (zone.Contains(x, y))
        {
            return true;
        }
    }
    return false;
}

public bool IsZoneFunctional(Zone zone)
{
    // A zone is considered functional if it has at least one passable tile
    for (int y = zone.Y; y < zone.Y + zone.Height; y++)
    {
        for (int x = zone.X; x < zone.X + zone.Width; x++)
        {
            if (IsPassable(x, y))
            {
                return true;
            }
        }
    }
    return false;
}
>>>>>>> REPLACE
NEXT: Implement a method to generate functional rooms.
