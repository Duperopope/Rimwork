CHANGE: Extract pawn registration logic into a separate method in Main.cs.
FILE: src/RimWorldGodot/Main.cs
<<<<<<< SEARCH
```csharp
foreach (string name in array)
{
    int num;
    int num2;
    do
    {
        num = _rng.Next(0, _world.Map.Width);
        num2 = _rng.Next(0, _world.Map.Height);
    }
    while (!_world.Map.IsPassable(num, num2));
    _world.RegisterThing(new Pawn(name, num, num2));
}
```
=======
```csharp
RegisterPawns(array);
```
>>>>>>> REPLACE
NEXT: Ensure the new method is correctly defined and called in both _Ready and _Process methods.
