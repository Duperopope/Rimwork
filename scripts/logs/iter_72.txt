CHANGE: Add a method to detect if a tile is part of a room.
FILE: src/RimWorldLab.Core/GameWorld.cs
<<<<<<< SEARCH
public bool IsPlantable(int x, int y)
{
    if (!IsPassable(x, y)) return false;
    if (_resources.Exists(r => r.X == x && r.Y == y)) return false;

    // Start of room detection logic
    if (IsTilePartOfRoom(x, y)) return false;
=======
public bool IsPlantable(int x, int y)
{
    if (!IsPassable(x, y)) return false;
    if (_resources.Exists(r => r.X == x && r.Y == y)) return false;

    // Start of room detection logic
    if (IsTilePartOfRoom(x, y)) return false;

    // Check if the tile is part of a room
    foreach (var zone in Zones)
    {
        if (zone.Contains(x, y))
        {
            return false;
        }
    }

    return true;
>>>>>>> REPLACE
NEXT: Implement a method to generate functional rooms automatically.
