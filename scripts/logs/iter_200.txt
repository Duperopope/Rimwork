CHANGE: Add a method to generate resources over time in GameWorld.cs
FILE: src/RimWorldLab.Core/GameWorld.cs
<<<<<<< SEARCH
public void GenerateResourcesOverTime()
{
    // Example: Generate resources based on some logic
    for (int x = 0; x < Width; x++)
    {
        for (int y = 0; y < Height; y++)
        {
            if (IsPlantable(x, y) && !_noGrowTiles.Contains((x, y)))
            {
                AddSapling(x, y, 100); // Add a sapling that takes 100 ticks to grow
            }
        }
    }
}
=======
public void GenerateResourcesOverTime()
{
    // Example: Generate resources based on some logic
    for (int x = 0; x < Width; x++)
    {
        for (int y = 0; y < Height; y++)
        {
            if (IsPlantable(x, y) && !_noGrowTiles.Contains((x, y)))
            {
                AddSapling(x, y, 100); // Add a sapling that takes 100 ticks to grow
            }
        }
    }

    // Generate stone resources in desert regions
    foreach (var region in _regions.Where(r => r.Biome == "Desert"))
    {
        for (int x = region.X; x < region.X + region.Width; x++)
        {
            for (int y = region.Y; y < region.Y + region.Height; y++)
            {
                if (IsPlantable(x, y) && !_noGrowTiles.Contains((x, y)))
                {
                    AddResource(ResourceKind.Rock, x, y);
                }
            }
        }
    }
}
>>>>>>> REPLACE
NEXT: Implement a method to generate wood resources in forest regions.
