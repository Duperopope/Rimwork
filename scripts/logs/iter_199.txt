CHANGE: Add a method to check if a tile is already occupied by a resource node.
FILE: src/RimWorldLab.Core/GameWorld.cs
<<<<<<< SEARCH
public bool IsPlantable(int x, int y)
{
    // Example: Check if the tile is not solid and not already occupied by a resource node
    return !_grid[x, y].IsSolid && !_resources.Any(r => r.X == x && r.Y == y);
}
=======
public bool IsPlantable(int x, int y)
{
    // Example: Check if the tile is not solid and not already occupied by a resource node
    return !_grid[x, y].IsSolid && !_resources.Any(r => r.X == x && r.Y == y) && !_saplings.Any(s => s.X == x && s.Y == y);
}
>>>>>>> REPLACE
NEXT: Implement a method to generate resources based on the tile type and biome.
