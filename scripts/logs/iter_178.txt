CHANGE: Add a method to check if a furniture arrangement forms a functional room.
FILE: src/RimWorldLab.Core/GameWorld.cs
<<<<<<< SEARCH
public class GameWorld
{
    private Dictionary<ResourceKind, int> _recycledResources = new();
    private Dictionary<ResourceKind, int> _resourceRegenerationRates = new();
// ... (lines omitted) ...
=======
public class GameWorld
{
    private Dictionary<ResourceKind, int> _recycledResources = new();
    private Dictionary<ResourceKind, int> _resourceRegenerationRates = new();

    private bool IsFurnitureArrangementFunctional(IEnumerable<Furniture> furniture)
    {
        // Example: Check if there is at least one bed and one table in the arrangement
        bool hasBed = furniture.Any(f => f.Kind == FurnitureKind.Bed);
        bool hasTable = furniture.Any(f => f.Kind == FurnitureKind.Table);
        return hasBed && hasTable;
    }
// ... (lines omitted) ...
>>>>>>> REPLACE
NEXT: Implement a method to generate basic room layouts based on placed furniture.
